{
  "tools": [
    {
      "name": "assess_401k_employee_applicability",
      "description": "SPE gates for W-2 401(k) employee contribution applicability/recommend.",
      "input_schema": {
        "type": "object",
        "required": ["w2", "filing_status_code"],
        "properties": {
          "w2": { "type": "object" },
          "retirement": { "type": "object" },
          "filing_status_code": { "type": "integer" }
        }
      }
    },
    {
      "name": "estimate_401k_employee_savings",
      "description": "SPE projected tax savings for 401(k) EE STRATEGY_CHANGE.",
      "input_schema": {
        "type": "object",
        "required": ["w2"],
        "properties": {
          "w2": { "type": "object" },
          "retirement": { "type": "object" },
          "rates": { "type": "object" },
          "filing_status_code": { "type": "integer" },
          "strategy_change": { "type": ["number", "null"] }
        }
      }
    }
  ]
}
